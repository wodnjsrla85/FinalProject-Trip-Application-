import 'package:flutter/material.dart';

class FirestPage extends StatelessWidget {
  const FirestPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}