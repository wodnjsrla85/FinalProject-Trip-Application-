class BookingTrendData {
  BookingTrendData(this.date, this.bookingCount);
  final DateTime date;
  final int bookingCount;
}

class GenderData {
  GenderData(this.gender, this.count);
  final String gender;
  final int count;
}

class AgeGroupData {
  AgeGroupData(this.ageGroup, this.count);
  final String ageGroup;
  final int count;
}
